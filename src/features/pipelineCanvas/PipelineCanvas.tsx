import { Panel } from "@/ui/components";

export default function PipelineCanvas() {
  return (
    <Panel title="Pipeline Diagram" headerSize="xl">
      <div style={{ opacity: 0.7 }}>
        React Flow canvas goes here next step (fixed template A).
      </div>
    </Panel>
  );
}